// Ejemplo 1: for Each para imprimir elementos de una lista
const numbers = [1, 2, 3, 4, 5];

/*console.log(numbers.forEach(num => {
    
})
)*/

numbers.forEach(num => console.log(num))    

/*data.forEach(function(element){
    console.log(element);
});
*/